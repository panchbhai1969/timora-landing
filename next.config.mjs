/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: "export",
    // distDir: "out", // Output folder
    // images: {
    //   unoptimized: true, // Required for images to work on static hosting
    // },
  };
export default nextConfig;
